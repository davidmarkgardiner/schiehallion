---
allowed-tools: Read, Write, MultiEdit, Bash, shadcn-ui, context7, taskmaster-ai, firecrawl-mcp, playright
description: invoke the scrape sub-agent and return data from website
---

Your job is to invoke the scrape sub-agent LOCATED: `.claude/agents/scrape.md` and return data from website: `$ARGUMENTS`
